extends layout 
block content 
    h1 Genre: #{genre.name} 

    div 


        dl
            each book in genre_book
                dt 
                    a(href=book.url) #{book.title}
                dd (#{book.summary})
                
            else 
                p This genre has no books.